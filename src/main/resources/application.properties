# Database Configuration
# Using localhost (127.0.0.1) for standard PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:5432/mymemory_db
spring.datasource.username=postgres
spring.datasource.password=Makara@2025
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# HHH90000025: PostgreSQLDialect does not need to be specified explicitly (but keeping it for clarity)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Disable default Spring Security user
spring.security.user.name=disabled
spring.security.user.password=disabled

# JWT Secret (MUST be a valid, long Base64 string)
# NEW FIX: Use a proper Base64 encoded key to avoid DecodingException.
jwt.secret=NDkzOWU2ZjM3OTk1NGMzM2VkMzQ5Mjk0YjE3NjdmNTU4Mjc4NTcwMWE3YTU1NDkwZTAyZDM0ZDc1OWQ2NzJjYg==

# Swagger Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html